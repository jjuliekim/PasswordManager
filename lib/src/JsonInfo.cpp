//
// Created by julie on 11/27/2023.
//

#include "JsonInfo.h"

/*
JsonInfo::JsonInfo(map<string, Data> info) {
    this->info = info;
}

map<string, Data> JsonInfo::getInfo() {
    return info;
}

void JsonInfo::setInfo(map<string, Data> info) {
    this->info = info;
}
*/
